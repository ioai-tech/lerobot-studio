# Legacy source tree (do not use)

This `src/` directory is a pre-monorepo leftover. Authoritative sources live in:

- `packages/core`
- `packages/platform`
- `packages/react`
- `packages/ui`
- `apps/web`

It is ignored by ESLint and is not part of the TypeScript project references.
Delete this tree once migration verification is complete.
